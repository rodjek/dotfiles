version: 5
defaults:
    datadir: data
    data_hash: yaml_data
hierarchy:
    - name: "Node specific data"
      path: "nodes/%{facts.hostname}.yaml"
    - name: "OS specific app data"
      glob: "os/%{facts.os.family}/*.yaml"
    - name: "OS specific data"
      path: "os/%{facts.os.family}.yaml"
    - name: "Common data"
      path: "common.yaml"
