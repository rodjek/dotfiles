XDG_CONFIG_HOME="${HOME}/.config"
XDG_CACHE_HOME="${HOME}/.cache"
XDG_DATA_HOME="${HOME}/.local/share"

PATH=~/.bin:$PATH:$HOME/go/bin
EDITOR=nvim
VISUAL=nvim
FZF_DEFAULT_COMMAND="rg --files"
OSBUDDY_VMARGS="-Duser.home=/home/tsharpe/.local/share/osbuddy"
TMPDIR=/tmp
BORG_REPO="borg-nas:/volume6/BorgBackup/semirhage"
BORG_PASSCOMMAND="pass borg-nas/semirhage"

SSH_AUTH_SOCK="$(gpgconf --list-dirs agent-ssh-socket)"

LESSHISTFILE="${XDG_CACHE_HOME}/less/history"
LESSKEY="${XDG_CONFIG_HOME}/less/key"

WGETRC="${XDG_CONFIG_HOME}/wget/wgetrc"

VAGRANT_HOME="${XDG_DATA_HOME}/vagrant"
VAGRANT_ALIAS_FILE="${XDG_DATA_HOME}/vagrant/aliases"

RUSTUP_HOME="${XDG_DATA_HOME}/rustup"
CARGO_HOME="${XDG_DATA_HOME}/cargo"
PATH="${PATH}:${CARGO_HOME}/bin"

ROSWELL_HOME="${XDG_DATA_HOME}/roswell"

IRBRC="${XDG_CONFIG_HOME}/irb/irbrc"

REDISCLI_HISTFILE="${XDG_DATA_HOME}/redis-cli/history"

RANDFILE="${XDG_CACHE_HOME}/openssl/rnd"

PYLINTHOME="${XDG_CACHE_HOME}/pylint"

PSQLRC="${XDG_CONFIG_HOME}/pg/psqlrc"
PSQL_HISTORY="${XDG_DATA_HOME}/pg/psql_history"
PGPASSFILE="${XDG_CONFIG_HOME}/pg/pgpass"
PGSERVICEFILE="${XDG_CONFIG_HOME}/pg/pg_service.conf"

PASSWORD_STORE_DIR="${XDG_DATA_HOME}/password-store"

NOTMUCH_CONFIG="${XDG_CONFIG_HOME}/notmuch/config"

NODE_REPL_HISTORY="${XDG_DATA_HOME}/node/history"

PYTHONSTARTUP="${XDG_CONFIG_HOME}/python/startup"

ELINKS_CONFDIR="${XDG_CONFIG_HOME}/elinks"

CCACHE_DIR="${XDG_DATA_HOME}/ccache"

MPLAYER_HOME="${XDG_CONFIG_HOME}/mplayer"
